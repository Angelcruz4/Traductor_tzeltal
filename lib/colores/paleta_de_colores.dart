import 'package:flutter/material.dart';

// --- Paleta de colores EtnoVox ---

// Colores principales extraídos del logotipo
const Color kPrimaryColor = Color(0xFF1E8463); // Verde esmeralda/teal (Salud y Naturaleza)
const Color kWineColor = Color(0xFF8B1D41);    // Color vino del texto "ETNOVOX" (Base y Profesionalismo)
const Color kAccentColor = Color(0xFFD44068);  // Rosa/Magenta vibrante (Acciones e interactividad)
const Color kYellowAccent = Color(0xFFF2B705); // Amarillo cálido (Resaltes y estados activos)

// Colores de fondo y superficies
const Color kLightColor = Color(0xFFFBF9F6);   // Fondo principal (Blanco hueso muy limpio/clínico)
const Color kCardColor = Colors.white;         // Fondo para las cajas de traducción

// Colores de texto y elementos UI
const Color kTextPrimary = Colors.white;       // Texto principal sobre fondos oscuros (verde o vino)
const Color kTextDark = Color(0xFF2C3E50);     // Texto oscuro (gris azulado oscuro) para mayor legibilidad
const Color kIconInactive = Colors.white60;    // Iconos inactivos en la barra inferior